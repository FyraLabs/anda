name: Rust
permissions:
  contents: read
on:
  pull_request:
  push:

jobs:
  rust:
    permissions:
      security-events: write
      contents: read
    uses: FyraLabs/actions/.github/workflows/rust.yml@main
