name: Rust
permissions:
  contents: read
on:
  pull_request:
  push:
    branches: ["main"]

jobs:
  rust:
    permissions:
      security-events: write
      contents: read
    uses: FyraLabs/actions/.github/workflows/rust.yml@main
